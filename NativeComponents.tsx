import {
	requireNativeComponent,
} from 'react-native';

export const MultilineTextBoxComponent = requireNativeComponent('MyMultilineTextBox');
export const MyListViewComponent = requireNativeComponent('MyListView');
